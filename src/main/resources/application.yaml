spring:
  application:
    name: store
  datasource:
    url: jdbc:mysql://localhost:3306/store_api
    username: root
    password:
  jpa:
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration  # Default location for SQL migrations
    baseline-on-migrate: true